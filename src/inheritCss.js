import  {css} from 'styled-components'


const width = "90%"
const pad = "25px"
const marg = "0 auto"

export default css`
  width: ${width};
  padding: ${pad};
  margin: ${marg};
  height: 100%;
  /* Material */
  border-radius: 5px;
  box-shadow: 0 4px 20px 0 rgba(0, 0, 0,.5), 0 7px 10px -5px rgba(244, 67, 54,.4);
`;